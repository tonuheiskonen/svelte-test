/* src\views\Help.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "../../_snowpack/pkg/svelte/internal.js";

function create_fragment(ctx) {
	let h1;
	let t1;
	let h2;
	let t3;
	let p0;
	let t5;
	let p1;
	let t7;
	let p2;
	let t9;
	let p3;

	return {
		c() {
			h1 = element("h1");
			h1.textContent = "Help page";
			t1 = space();
			h2 = element("h2");
			h2.textContent = "Excel";
			t3 = space();
			p0 = element("p");
			p0.textContent = "get files list";
			t5 = space();
			p1 = element("p");
			p1.textContent = "validate by file extension (only xlsx allowed)";
			t7 = space();
			p2 = element("p");
			p2.textContent = "validate each file for right layout";
			t9 = space();
			p3 = element("p");
			p3.textContent = "each file has to have strict layout with specific column names";
		},
		m(target, anchor) {
			insert(target, h1, anchor);
			insert(target, t1, anchor);
			insert(target, h2, anchor);
			insert(target, t3, anchor);
			insert(target, p0, anchor);
			insert(target, t5, anchor);
			insert(target, p1, anchor);
			insert(target, t7, anchor);
			insert(target, p2, anchor);
			insert(target, t9, anchor);
			insert(target, p3, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h1);
			if (detaching) detach(t1);
			if (detaching) detach(h2);
			if (detaching) detach(t3);
			if (detaching) detach(p0);
			if (detaching) detach(t5);
			if (detaching) detach(p1);
			if (detaching) detach(t7);
			if (detaching) detach(p2);
			if (detaching) detach(t9);
			if (detaching) detach(p3);
		}
	};
}

class Help extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Help;